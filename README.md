This project is a Delivery Management System developed using Python. The main objective of this program is to efficiently assign delivery packages to the nearest delivery agents and generate a detailed performance report.

The system reads data from a JSON file containing information about warehouses, delivery agents, and packages. Using the Euclidean Distance Formula, the program calculates the shortest distance between agents and warehouses to determine the most suitable agent for each delivery.
